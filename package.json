{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "A minimal Apps Script web app scaffold. Serves `src/index.html` via `doGet()` in `src/main.gs` using the V8 runtime.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^3.0.6-alpha"
  }
}
